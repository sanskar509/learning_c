#include<stdio.h>
   // Calculate the sum
int main() 
{
    float a, b, sum;
    printf("inter number");
    scanf("%f %f", &a, &b);

     sum = a + b;
    printf("sum is : %f", a + b);
    return 0;
}